# Poietek is fully usable without cloud or AI providers. Client-side code must
# never contain private provider keys. Configure optional providers through a
# trusted server/native adapter or a user-owned local configuration screen.

# Public, non-secret URL settings may use Vite's VITE_ prefix when needed.
# VITE_POIETEK_PUBLIC_APP_URL="https://studio.example.com"
